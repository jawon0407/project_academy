const swiper = new Swiper(".swiper", {
    // Optional parameters
    loop: true,
    initialSlide: 0,
    slidesPerView: "auto",
    centeredSlides: true,
    // If we need pagination
    pagination: {
      el: ".swiper-pagination",
    },
    // Navigation arrows
  });